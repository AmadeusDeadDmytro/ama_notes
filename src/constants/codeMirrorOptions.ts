const options = {
    mode: 'gfm',
    theme: 'base16-light',
    lineNumbers: true,
    lineWrapping: true,
    styleActiveLine: {
        nonEmpty: true,
    },
    viewportMargin: Infinity,
}

export default options
