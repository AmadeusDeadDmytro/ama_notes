language: node_js
node_js:
  - "12"
install:
  - "npm i"
script:
  - "npm run build"
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master